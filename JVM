Expert Java 8+ with deep JVM internals, GC, classloading, memory management, tuning
Advanced concurrency: Executor/ForkJoin, locks, concurrent collections, Java memory model, non-blocking programming
===================================================JVM ===============================================
The Java Virtual Machine (JVM) is  the core execution engine of Java, that translating platform-independent bytecode into machine-specific code.
JVM is a runtime engine that converts Java bytecode into machine-specific instructions, providing platform independence, memory management, and security.
The JVM architecture is split into three main subsystems: the ClassLoader Subsystem, the Runtime Data Areas (Memory), and the Execution Engine.

**ClassLoader Subsystem** loads `.class` files into memory in three stages: Bootstrap (loads core Java libs like `rt.jar`), Extension (loads from `ext` directory), 
and Application (loads your project's classpath). It follows the *parent delegation model* — always asks the parent before loading itself.

**Runtime Data Areas** (memory):

- *Method Area* — stores class-level data: metadata, static variables, constant pool. Shared across threads.
- *Heap* — where all objects and arrays live. Managed by the Garbage Collector. Also shared.
- *JVM Stack* — each thread has its own stack of frames. Each frame holds local variables and the operand stack for one method call.
- *PC Register* — per-thread pointer to the currently executing instruction.
- *Native Method Stack* — like the JVM stack but for native (C/C++) methods.

**Execution Engine** is where bytecode actually runs:

- *Interpreter* — reads and executes bytecode one instruction at a time. Simple but slow.
- *JIT Compiler* — detects "hot" code (frequently run) and compiles it to native machine code for much faster execution.
- *Garbage Collector* — automatically reclaims heap memory from unreachable objects. Common algorithms: Serial, Parallel, G1, ZGC.

**JNI (Java Native Interface)** — the bridge that allows Java to call native C/C++ libraries and vice versa.

**Key interview gotchas:**
- Stack memory is thread-safe (each thread has its own); Heap is shared and needs synchronization.
- `StackOverflowError` = stack full (usually infinite recursion); `OutOfMemoryError` = heap full.
- JIT is why Java isn't as slow as interpreted languages — it warms up over time.
- `String` literals go into the *String Pool* inside the Heap (since Java 7+).

============================================Garbage Collection===========================================
Que: What is MetadataSpace ?
Ans: It has replaced the older PermGen memory space.

Que: What Is Garbage Collection and What Are Its Advantages?
Ans: Garbage collection is the process of looking at heap memory, identifying which objects are in use and which are not, and deleting the unused objects.

Que: Are There Any Disadvantages of Garbage Collection?
Yes. Whenever the garbage collector runs, it has an effect on the application's performance. This is because all other threads in the application have to be 
stopped to allow the garbage collector thread to effectively do its work.

Que: What is Heap memory?
Ans: The heap is a large bulk of memory intended for allocation of objects. When you create an object with the new keyword, it gets allocated on the heap. 
However, the reference to this object lives on the stack.

Que: Java hep in shor way?
Ans:Java heap is split into Young Gen (Eden + two Survivors) and Old Gen. New objects start in Eden, survive GC cycles through Survivors, 
and eventually get promoted to Old Gen. Metaspace (outside heap) holds class metadata since Java 8.

Que: Describe how Garbage Collection Works ?
Ans: Garbage Collection (GC) in Java is an automatic memory management process executed by the Java Virtual Machine (JVM) to look for, identify, and destroy 
dynamically allocated heap objects that are no longer referenced by the application.
The JVM generally relies on a Mark-Sweep-Compact framework to clean up memory.
Marking: The GC traces the object graph starting from active references called GC Roots (such as thread stacks, local variables, and static fields). 
It marks all reachable objects as "live".
Sweeping: The GC scans the heap memory to identify unmarked objects. It unallocates the memory spaces occupied by these "dead" objects.
Compacting: To prevent memory fragmentation, the GC shifts the remaining live objects into contiguous blocks of memory. This makes allocating space for future objects much faster.

The heap is divided up into smaller spaces or generations. These spaces are Young Generation, Old or Tenured Generation.
The young generation hosts most of the newly created objects.The young generation space is further divided into three spaces: an Eden space and two 
survivor spaces such as Survivor 1 (s1) and Survivor 2 (s2).The old generation hosts objects that have lived in memory longer than a certain “age”. 
The objects that survived garbage collection from the young generation are promoted to this space.

First, any new objects are allocated to the Eden space. Both survivor spaces start out empty. When the Eden space fills up, a minor garbage collection is 
triggered. Referenced objects are moved to the first survivor space. Unreferenced objects are deleted.During the next minor GC, the same thing happens to the 
Eden space. Unreferenced objects are deleted and referenced objects are moved to a survivor space. However, in this case, they are moved to the second survivor
 space (S2).
In addition, objects from the last minor GC in the first survivor space (S1) have their age incremented and are moved to S2. Once all surviving objects have
been moved to S2, both S1 and Eden space are cleared. At this point, S2 contains objects with different ages.
At the next minor GC, the same process is repeated. However this time the survivor spaces switch. Referenced objects are moved to S1 from both Eden and S2. 
Surviving objects are aged. Eden and S2 are cleared.
After every minor garbage collection cycle, the age of each object is checked. Those that have reached a certain arbitrary age, for example, 8, are promoted 
from the young generation to the old or tenured generation. For all subsequent minor GC cycles, objects will continue to be promoted to the old generation space.
This pretty much exhausts the process of garbage collection in the young generation. Eventually, a major garbage collection will be performed on the old 
generation which cleans up and compacts that space. For each major GC, there are several minor GCs.
